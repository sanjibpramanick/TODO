
To start this application, execute the following command
mvn clean install spring-boot:run

To access the Database, access the following URL,
http://<host>:<running-port>/h2

To access the application, access the following URL,
http://<host>:<port>/welcome